# 🚀 Quick Start Guide - Smart Breath Analyzer

## การติดตั้งและรันครั้งแรก

### ขั้นตอนที่ 1: ติดตั้ง Dependencies

```bash
pip install flask psutil --break-system-packages
```

หรือใช้ requirements.txt (ถ้ามี):
```bash
pip install -r requirements.txt --break-system-packages
```

### ขั้นตอนที่ 2: เตรียมโครงสร้างไฟล์

ตรวจสอบว่ามีโครงสร้างดังนี้:

```
smart-breath-analyzer/
├── app.py
└── templates/
    ├── login.html
    ├── intro.html
    ├── index.html
    ├── page2.html
    ├── end.html
    └── user.html
```

### ขั้นตอนที่ 3: รันเซิร์ฟเวอร์

```bash
python3 app.py
```

คุณจะเห็นข้อความ:
```
 * Running on http://0.0.0.0:5022
 * Running on http://127.0.0.1:5022
```

### ขั้นตอนที่ 4: เข้าใช้งาน

เปิดเบราว์เซอร์และไปที่:
```
http://localhost:5022
```

---

## 🔐 การใช้งานครั้งแรก

### 1. Login ด้วย Default Account

หน้าแรกที่จะเห็นคือหน้า Login:

- **Username:** `admin`
- **Password:** `1234`

กดปุ่ม **"เข้าสู่ระบบ"**

### 2. หรือ Register Account ใหม่

ถ้าต้องการสร้าง account ใหม่:

1. คลิก **"ลงทะเบียนใหม่"**
2. กรอก Username และ Password ที่ต้องการ
3. กดปุ่ม **"สมัครสมาชิก"**
4. จะกลับไปหน้า Login อัตโนมัติ
5. Login ด้วย account ที่เพิ่งสร้าง

---

## 📱 การใช้งานระบบ

### Flow การใช้งานปกติ:

```
1. Login → 2. Intro → 3. Dashboard → 4. Analysis → 5. End → กลับ Intro
```

### รายละเอียดแต่ละหน้า:

#### 🏠 หน้า Intro
- แสดงข้อมูลโปรเจ็กต์
- แสดง Username มุมขวาบน (👤)
- มีปุ่ม **"ออกจากระบบ"** (🚪)
- กดปุ่ม **"เข้าสู่ระบบเพื่อเริ่มใช้งาน"** → ไป Dashboard

#### 📊 หน้า Dashboard
- แสดงข้อมูลเซนเซอร์แบบเรียลไทม์
- แสดง Username มุมขวาบน
- สามารถ:
  - กดปุ่ม **"เริ่มเก็บข้อมูล"** เพื่อเริ่มการวัด
  - กดปุ่ม **"หยุดการวัด"** เพื่อหยุดการวัด
  - ดูกราฟและค่าต่างๆ
  - กดปุ่ม **"ดูผลการวิเคราะห์"** → ไป Page2

#### 📈 หน้า Analysis (Page2)
- แสดงผลการวิเคราะห์
- ประเมินความเสี่ยงด้านสุขภาพ
- แสดง Username มุมขวาบน
- กดปุ่ม **"ดูสรุป"** → ไป End

#### 🏁 หน้า End
- แสดงสรุปและข้อมูลผู้พัฒนา
- แสดง Username มุมขวาบน
- กดปุ่ม **"เริ่มใหม่ (Back to Intro)"** → กลับไป Intro

**⚠️ สำคัญ:** เมื่อกดปุ่ม "เริ่มใหม่" จะกลับไปหน้า Intro โดยยังคง Login อยู่ ไม่ต้อง Login ใหม่!

#### 🚪 การออกจากระบบ (Logout)
- กดปุ่ม **"🚪 ออกจากระบบ"** ที่มุมขวาบนของทุกหน้า
- จะมีการถามยืนยัน: "ต้องการออกจากระบบ?"
- กด **OK** → กลับไปหน้า Login

---

## 🔄 Use Cases

### Use Case 1: ใช้งานปกติ (Loop)

```
1. Login
2. ไป Intro
3. ไป Dashboard → เก็บข้อมูล
4. ไป Analysis → ดูผล
5. ไป End → กด "เริ่มใหม่"
6. กลับ Intro (ยังคง Login อยู่)
7. วนลูปไปที่ขั้นตอน 3
```

### Use Case 2: ออกจากระบบ

```
1. อยู่ที่หน้าใดก็ได้
2. กดปุ่ม "🚪 ออกจากระบบ" มุมขวาบน
3. ยืนยัน
4. กลับไปหน้า Login
```

### Use Case 3: เปลี่ยนผู้ใช้

```
1. Logout จาก user คนปัจจุบัน
2. Login ด้วย user อื่น
```

---

## ⚙️ การปรับแต่ง

### เปลี่ยนพอร์ตของเซิร์ฟเวอร์

แก้ไขไฟล์ `app.py` บรรทัดสุดท้าย:

```python
app.run(host='0.0.0.0', port=5022, debug=True)
```

เปลี่ยน `5022` เป็นพอร์ตที่ต้องการ เช่น `5000`, `8080`

### เพิ่มผู้ใช้ Default

แก้ไขไฟล์ `app.py` ในส่วน `load_users()`:

```python
def load_users():
    if os.path.exists(USER_DATA_FILE):
        with open(USER_DATA_FILE, 'r', encoding='utf-8') as f:
            return json.load(f)
    return {
        "admin": "1234",
        "user1": "password1",  # เพิ่มผู้ใช้ใหม่
        "user2": "password2"   # เพิ่มผู้ใช้ใหม่
    }
```

### เปลี่ยนธีมสี

แก้ไขไฟล์ HTML ในส่วน CSS:

```css
:root {
    --primary: #123580;      /* สีหลัก - เปลี่ยนเป็นสีที่ต้องการ */
    --accent: #5e81ac;       /* สีเสริม */
    --bg: #eef2f7;          /* สีพื้นหลัง */
}
```

---

## 🐛 การแก้ไขปัญหาทั่วไป

### ปัญหา: ไม่สามารถ Login ได้

**วิธีแก้:**
1. ตรวจสอบว่ากรอก Username และ Password ถูกต้อง
2. ลอง Login ด้วย default: `admin` / `1234`
3. ตรวจสอบว่าไฟล์ `users.json` ไม่ corrupt
4. ลบไฟล์ `users.json` แล้วรันใหม่

### ปัญหา: หลัง Login แล้วกลับมาหน้า Login อีก

**วิธีแก้:**
1. ตรวจสอบว่า Flask session ทำงานปกติ
2. ดูที่ console ว่ามี error หรือไม่
3. ตรวจสอบว่า `app.secret_key` ตั้งค่าแล้ว

### ปัญหา: กด Logout แล้วยังเข้าหน้าอื่นได้

**วิธีแก้:**
1. Clear cache ของ browser
2. ปิด browser แล้วเปิดใหม่
3. ใช้ Incognito/Private mode

### ปัญหา: ไม่เห็น Username มุมขวาบน

**วิธีแก้:**
1. ตรวจสอบว่า Login สำเร็จจริงๆ
2. ตรวจสอบว่าไฟล์ template ถูกต้อง
3. ดูที่ browser console ว่ามี error หรือไม่

---

## 📞 ติดต่อและสนับสนุน

หากมีปัญหาหรือข้อสงสัย ติดต่อผู้พัฒนา:

- **Mr. Podchara Pribwai**
  - Email: podchara.pri@pccpl.ac.th
  - Tel: 089-854-8927

- **Ms. Wannida Chantarakun**
  - Email: wannida.cha@pccpl.ac.th
  - Tel: 096-662-2027

- **Ms. Sicha Luechoowong**
  - Email: sicha.lue@pccpl.ac.th
  - Tel: 080-9627-449

---

## ✅ Checklist ก่อนใช้งาน

- [ ] ติดตั้ง Python 3.x แล้ว
- [ ] ติดตั้ง Flask และ psutil แล้ว
- [ ] มีโครงสร้างไฟล์ครบถ้วน
- [ ] รัน `python3 app.py` สำเร็จ
- [ ] เปิด browser ได้แล้ว
- [ ] Login ได้แล้ว

---

## 🎉 พร้อมใช้งาน!

ตอนนี้คุณพร้อมใช้งาน Smart Breath Analyzer แล้ว!

ขั้นตอนง่ายๆ:
1. Login
2. เริ่มใช้งาน
3. วิเคราะห์ผล
4. วนลูปได้ไม่จำกัด!

---

© 2026 Smart Breath Analyzer Project | วิชาคอมพิวเตอร์ 6 (ว23202)
