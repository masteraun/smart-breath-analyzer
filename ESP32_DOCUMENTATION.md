# 📡 ESP32 Smart Breath Analyzer - คู่มือการใช้งาน

## 🎯 ภาพรวมการทำงาน

ESP32 จะทำหน้าที่:
1. อ่านค่าจากเซนเซอร์ทั้งหมด (MQ2, MQ3, MQ7, MQ8, MQ135, DHT22)
2. คำนวณค่า PPM และกรองสัญญาณด้วย Exponential Moving Average
3. ส่งข้อมูลไปยัง Flask Server ทุก 5 วินาที
4. รับสถานะ `is_measuring` จาก Server
5. แสดงผลบน LCD 16x2 ตามสถานะ

---

## 🔧 การตั้งค่า Hardware

### Pin Connections

| เซนเซอร์/อุปกรณ์ | ESP32 Pin |
|-----------------|-----------|
| MQ2             | GPIO 33   |
| MQ3             | GPIO 32   |
| MQ7             | GPIO 35   |
| MQ8             | GPIO 34   |
| MQ135           | GPIO 36   |
| DHT22 Data      | GPIO 27   |
| Relay 1         | GPIO 26   |
| Relay 2         | GPIO 25   |
| LCD SDA         | GPIO 21   |
| LCD SCL         | GPIO 22   |

### LCD I2C
- **Address:** 0x27
- **Size:** 16x2

---

## 📺 LCD Display Modes

### 1. 🟢 เปิดเครื่อง (Boot Up)

```
  Welcome to    
Smart breath    
```
แสดง 3-4 วินาที

---

### 2. 📶 เชื่อมต่อ WiFi

```
Connecting WiFi
................
```
แสดงจุดไข่ปลาวิ่งจนกว่าจะเชื่อมต่อสำเร็จ

```
WiFi Connected!
                
```
แสดง 1.5 วินาที

---

### 3. 🔧 Calibration Mode (60 วินาที)

```
Calibrating...  
                
```

จุดไข่ปลาจะวิ่งแบบ: `ไม่มี → . → .. → ... → .. → . → ไม่มี` (วนลูป)

เสร็จแล้วแสดง:
```
Calibrate Done! 
                
```
แสดง 2 วินาที

จากนั้นกลับไปแสดง:
```
  Welcome to    
Smart breath    
```

---

### 4. 🔬 กำลังวัด (Measuring Mode)

เมื่อผู้ใช้กดปุ่ม **"เริ่มเก็บข้อมูล"** บนหน้าเว็บ (Dashboard)

LCD จะสลับแสดงผล **ทุก 10 วินาที** ใน 3 หน้า:

#### หน้าที่ 1: อุณหภูมิและความชื้น
```
Calculating...  
T:25.3C H:65%   
```

#### หน้าที่ 2: MQ2, MQ3, MQ7
```
Calculating...  
2:45 3:12 7:8   
```

#### หน้าที่ 3: MQ8, MQ135
```
Calculating...  
8:23 135:67 ppm 
```

**หมายเหตุ:** 
- ค่า PPM จะปัดเป็นจำนวนเต็ม
- "Calculating..." จะอยู่บรรทัดแรกตลอดเวลา

---

### 5. ✅ การวัดเสร็จสิ้น (Done)

เมื่อผู้ใช้กดปุ่ม **"หยุดการวัด"** บนหน้าเว็บ หรือไปยังหน้า Page2/End

LCD จะแสดง:

```
Calculating Done
Status: Normal  
```

หรือ

```
Calculating Done
Risk: Alcohol   
```

**ค้างแสดงผลนี้** จนกว่าผู้ใช้จะ:
- กลับไปหน้า Intro (กดปุ่ม "เริ่มใหม่" ที่หน้า End)
- เริ่มการวัดใหม่

---

### 6. 🔄 กลับหน้า Intro

เมื่อผู้ใช้กดปุ่ม **"เริ่มใหม่"** ที่หน้า End

LCD จะกลับมาแสดง:

```
  Welcome to    
Smart breath    
```

พร้อมรับการวัดครั้งต่อไป

---

## 📊 Serial Monitor Output

Serial Monitor จะแสดงข้อมูลละเอียดทุก 5 วินาที:

```
======================================================
ENV | Temp: 25.3 C | Humid: 65.2 %
------------------------------------------------------
SENSOR  |  RAW ADC  |   Rs (kOhm)  |  Ratio  |   PPM
MQ2     |  2345     |   1.23       |  1.234  |   45.67
MQ3     |  1890     |   2.45       |  0.041  |   12.34
MQ7     |  3456     |   0.89       |  0.033  |   8.90
MQ8     |  2100     |   1.67       |  0.024  |   23.45
MQ135   |  2890     |   1.98       |  0.550  |   67.89
------------------------------------------------------
Risk Assessment: Status: Normal
======================================================
```

---

## 🎨 Flow Chart

```
┌─────────────────┐
│   Boot Up       │
│ "Welcome to"    │
└────────┬────────┘
         ↓
┌─────────────────┐
│ Connect WiFi    │
│ "Connecting..." │
└────────┬────────┘
         ↓
┌─────────────────┐
│ Calibration     │
│ "Calibrating.." │ (60 วินาที)
└────────┬────────┘
         ↓
┌─────────────────┐
│   Welcome       │ ← สถานะพัก (Idle)
│ "Welcome to"    │
└────────┬────────┘
         ↓
    (รอคำสั่งจากเว็บ)
         ↓
┌─────────────────┐
│  Measuring      │ ← เริ่มการวัด
│ "Calculating..."│
│ สลับ 3 หน้า     │
└────────┬────────┘
         ↓
┌─────────────────┐
│     Done        │ ← หยุดการวัด
│ "Calculating    │
│       Done"     │
│ แสดงผลประเมิน   │
└────────┬────────┘
         ↓
    (กลับหน้า Intro)
         ↓
┌─────────────────┐
│   Welcome       │
│ (วนลูป)         │
└─────────────────┘
```

---

## 🔐 การประเมินความเสี่ยง (Risk Assessment)

ระบบจะประเมินตามลำดับความสำคัญ:

| ลำดับ | เงื่อนไข | ข้อความที่แสดง |
|-------|---------|---------------|
| 1 | MQ3 > 50 | `Risk: Alcohol` |
| 2 | MQ135 > 150 | `Risk: Kidney/Amm` |
| 3 | MQ135 > 100 AND MQ2 > 50 | `Risk: Ketosis` |
| 4 | MQ8 > 50 | `Risk: Digestion` |
| 5 | MQ2 > 30 | `Caution: Smoke` |
| 6 | MQ7 > 20 | `Caution: CO` |
| 7 | ไม่ตรงเงื่อนไขใดๆ | `Status: Normal` |

---

## 🌐 การสื่อสารกับ Server

### HTTP POST Request (ทุก 5 วินาที)

**URL:** `http://172.16.12.103:5022/api/status`

**Request Body:**
```json
{
  "dht_temperature": 25.3,
  "humidity": 65.2,
  "sensors": {
    "mq2_ppm": 45.67,
    "mq3_ppm": 12.34,
    "mq7_ppm": 8.90,
    "mq8_ppm": 23.45,
    "mq135_ppm": 67.89
  }
}
```

**Response:**
```json
{
  "is_measuring": true
}
```

---

## ⚙️ การตั้งค่าที่สำคัญ

### WiFi Settings
```cpp
const char* ssid     = "@PCCPL-WIFI"; 
const char* password = "";
const char* serverName = "http://172.16.12.103:5022/api/status";
```

### Timing
- **ส่งข้อมูล:** ทุก 5 วินาที (`lastMsg`)
- **สลับหน้า LCD:** ทุก 10 วินาที (`lastLcdUpdate`)
- **Calibration:** 60 วินาที

### Exponential Moving Average
```cpp
float alpha = 0.2; // ความเร็วในการตอบสนอง (0.0-1.0)
```

---

## 🔍 Troubleshooting

### ปัญหา: LCD ไม่แสดงผล
- ตรวจสอบ I2C Address (ลอง 0x27 หรือ 0x3F)
- ตรวจสอบสาย SDA, SCL
- ปรับความสว่าง LCD

### ปัญหา: WiFi ไม่เชื่อมต่อ
- ตรวจสอบ SSID และ Password
- ตรวจสอบว่า ESP32 อยู่ในระยะ WiFi
- ดู Serial Monitor หาข้อผิดพลาด

### ปัญหา: ค่าเซนเซอร์ผิดปกติ
- ให้เวลา Warm-up อย่างน้อย 5 นาที
- ตรวจสอบการเชื่อมต่อเซนเซอร์
- ทำ Calibration ใหม่

### ปัญหา: LCD ไม่เปลี่ยนสถานะ
- ตรวจสอบว่า Server ทำงานปกติ
- ดู Serial Monitor ว่ารับ `is_measuring` ถูกต้องหรือไม่
- ตรวจสอบการเชื่อมต่อเครือข่าย

---

## 📝 การปรับแต่ง

### เปลี่ยน Server IP
แก้ไขบรรทัด:
```cpp
const char* serverName = "http://YOUR_SERVER_IP:5022/api/status";
```

### เปลี่ยนเวลาสลับหน้า LCD
แก้ไขบรรทัด:
```cpp
if (now - lastLcdUpdate > 10000) { // 10000 = 10 วินาที
```

### เปลี่ยนข้อความแสดงความเสี่ยง
แก้ไขฟังก์ชัน `getRiskAssessment()`

---

## ✅ Checklist ก่อนใช้งาน

- [ ] ตรวจสอบการเชื่อมต่อเซนเซอร์ทั้งหมด
- [ ] ตรวจสอบการเชื่อมต่อ LCD I2C
- [ ] ตั้งค่า WiFi SSID และ Password
- [ ] ตั้งค่า Server IP ให้ถูกต้อง
- [ ] Upload โค้ดลง ESP32
- [ ] เปิด Serial Monitor (115200 baud)
- [ ] รอ Calibration เสร็จสิ้น (60 วินาที)
- [ ] ทดสอบการส่งข้อมูลไปยัง Server

---

## 🎓 ข้อมูลเพิ่มเติม

**โปรเจ็กต์:** Smart Breath Analyzer  
**วิชา:** คอมพิวเตอร์ 6 (ว23202)

**ผู้พัฒนา:**
- Mr. Podchara Pribwai - ม.3/1 เลขที่ 5
- Ms. Wannida Chantarakun - ม.3/1 เลขที่ 20
- Ms. Sicha Luechoowong - ม.3/1 เลขที่ 22

---

© 2026 Smart Breath Analyzer Project
